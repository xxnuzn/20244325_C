program1 실행결과:
10
10
10

program2 실행결과:
10
11
12

차이점과 이유:
program1은 int index = 10 으로 선언해서 함수가 호출될때마다 index가 10으로 다시 초기화된다.
그래서 매번 10이 출력된다.

program2는 static int index = 10 으로 선언해서 처음 한번만 10으로 초기화되고
함수가 끝나도 값이 사라지지 않고 유지된다.
그래서 index++가 누적되어 10, 11, 12가 출력된다.

static 지역변수는 프로그램이 끝날때까지 메모리에 남아있다.
